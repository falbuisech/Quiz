
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Quiz</title>
    <link rel='stylesheet' type="text/css" href='/stylesheets/style.css' />
    <link rel='stylesheet' type="text/css" href='/stylesheets/smartphone.css'
      media="only screen and (max-width: 480)"/>
    <link rel='stylesheet' type="text/css" href='/stylesheets/wide.css'
      media= "only screen and (min-width: 1920) and (min-device-width: 2000)"/>

  </head>
  <body>
    <div id="page-wrap">
      <header class="main" id="h1">
        <h2>Quiz<span>: El juego de las preguntas</span></h2>
      </header>

      <nav class="main" id="n1" role="navigation">
        <span><a href="/">Inicio</a></span>
        <span><a href="/quizes">Preguntas</a></span>
        <%if(session.user){%>
          <span><a href="/user/<%=session.user.id%>/quizes">Mis preguntas</a></span>
        <%}%>
	      <span><a href="/author">Autores</a></span>
        <span><a href="/quizes/stats">Estadísticas</a></span>
   <%if(!session.user) {%>
        <span class="right">
              <a href="/user">Registrarse</a>
              <a href="/login">Entrar</a>
            </span>
   <%} else{%>
       <span class="right"> 
            <%=session.user.username%>
            <a href="/user/<%=session.user.id%>/edit">Settings</a>
            <a href="/logout"> Cerrar sesión</a></span>
   <%}%>
      </nav>

      <section class="main" id="s1">
        <% if(errors.length){ %>
          <span id="ErrorMsgs">
            Hay errores: <br/>
            <% for(var i in errors){ %>
              <span> <%= errors[i].message %> <br/></span>
            <% } %>
          </span>
        <% } %>
      <div><%- body %></div>
      </section>

      <footer class="main" id="f1">
        <a href="http://es.wikipedia.org/wiki/Quiz">¿Qué es un Quiz?</a>
      </footer>

    </div>
  </body>
